#include <iostream>
#include <cstdio>
#include <stack>
//#include <string>
using namespace std;

int main(){
	int input, i, j;
	string array;
	vector<int> v;
	
	cin >> array;
	
	if(array[i] == "(" ) v.push(i);
	if(array[i+1] == ")") v.pop()
		
	
	// (별 가중치를 더해 스택으로 저장
	// LIFO
	// stack;
	//pop / push 
	
}
